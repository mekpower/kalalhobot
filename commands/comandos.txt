Comandos de servidor 🛃:

`ping` , `serverinfo` , `level` , `reports` , `bugr` , `botinfo`

Games:

`osu`, `fort`

Aleatórios 🧙‍:

`frita` , `troll` , `change` , `proverbio`

Música 🎼:

`tocar` , `pular`

